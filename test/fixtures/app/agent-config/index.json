{
  "cases": [
    "kilo__fresh",
    "kilo__rerun",
    "kilo__empty_key",
    "kilo__existing_other_provider",
    "kilo__jsonc_comments_dropped",
    "kilo__empty_file",
    "kilo__provider_not_object_replaced",
    "kilo__custom_schema_preserved",
    "claude_code__fresh",
    "claude_code__rerun",
    "claude_code__empty_key",
    "claude_code__empty_model",
    "claude_code__existing_settings_preserved",
    "claude_code__env_not_object_replaced",
    "claude_code__rerun_empty_model_keeps_old_model_keys",
    "pi__fresh",
    "pi__rerun",
    "pi__empty_key",
    "pi__existing_other_provider",
    "pi__providers_not_object_replaced",
    "codex__fresh",
    "codex__rerun",
    "codex__empty_key",
    "codex__existing_toml_preserved",
    "codex__existing_managed_block_replaced",
    "codex__whitespace_only_file",
    "codex__model_needing_toml_escape",
    "codex__keyed_then_keyless_rerun",
    "opencode__fresh",
    "opencode__rerun",
    "opencode__empty_key",
    "opencode__existing_other_provider",
    "opencode__empty_file",
    "opencode__provider_not_object_replaced",
    "openclaude__fresh",
    "openclaude__rerun",
    "openclaude__empty_key_same_output",
    "openclaude__existing_profiles_preserved",
    "openclaude__matching_profile_replaced_in_place",
    "openclaude__profiles_not_array_replaced",
    "cline__auth_success",
    "cline__auth_success_empty_key",
    "cline__auth_command_fails",
    "cline__binary_missing",
    "dsh__fresh",
    "dsh__rerun",
    "dsh__empty_key_writes_no_env",
    "dsh__keyed_then_keyless_clears_env",
    "dsh__existing_yaml_preserved",
    "dsh__existing_atomic_route_replaced_wholesale",
    "dsh__backup_written_once",
    "dsh__existing_env_preserved",
    "dsh__comment_only_yaml",
    "dsh__empty_url_rejected",
    "dsh__empty_model_rejected",
    "dsh__key_with_whitespace_rejected",
    "dsh__key_with_hash_rejected",
    "zed__fresh",
    "zed__rerun",
    "zed__empty_model",
    "zed__empty_key_same_output",
    "zed__existing_settings_preserved",
    "zed__language_models_not_object_replaced",
    "mimo__fresh",
    "mimo__rerun",
    "mimo__empty_key",
    "mimo__existing_other_provider",
    "droid__fresh",
    "droid__rerun",
    "droid__empty_key",
    "droid__appended_after_existing_models",
    "droid__existing_atomic_entry_replaced_at_its_index",
    "droid__custom_models_not_array_replaced",
    "copilot__fresh_zsh",
    "copilot__rerun_zsh",
    "copilot__empty_key_omits_key_var",
    "copilot__existing_rc_preserved",
    "copilot__stray_prefixed_export_removed",
    "copilot__fresh_bash",
    "copilot__keyed_then_keyless_rerun",
    "openhands__fresh_zsh",
    "openhands__rerun_zsh",
    "openhands__empty_key_uses_placeholder",
    "openhands__existing_rc_preserved",
    "openhands__fresh_bash",
    "poolside__fresh_zsh",
    "poolside__rerun_zsh",
    "poolside__empty_key_uses_placeholder",
    "poolside__url_without_v1",
    "poolside__url_with_trailing_slash",
    "poolside__url_with_padding",
    "poolside__existing_rc_preserved",
    "goose__fresh_zsh",
    "goose__rerun_zsh",
    "goose__empty_key_uses_placeholder",
    "goose__unrelated_openai_export_preserved",
    "goose__fresh_bash",
    "muse__fresh_zsh",
    "muse__rerun_zsh",
    "muse__empty_key_uses_placeholder",
    "muse__existing_rc_preserved",
    "atomic_agent__fresh",
    "atomic_agent__rerun",
    "atomic_agent__empty_key",
    "atomic_agent__empty_model_rejected",
    "atomic_agent__existing_providers_preserved",
    "atomic_agent__dangling_embedding_provider_repaired",
    "atomic_agent__managed_mode_seeds_daemon_url",
    "atomic_agent__managed_mode_default_port",
    "atomic_agent__embeddings_enabled_splits_base_url",
    "atomic_agent__embeddings_explicit_url",
    "atomic_agent__existing_timeout_on_our_entry_preserved",
    "atomic_agent__llm_not_object_replaced",
    "hermes__fresh_seeds_skeleton",
    "hermes__rerun",
    "hermes__empty_key_same_output",
    "hermes__existing_config_other_providers_preserved",
    "hermes__existing_atomic_provider_replaced",
    "hermes__existing_provider_timeout_preserved",
    "hermes__env_gets_no_proxy",
    "hermes__env_with_no_proxy_untouched",
    "hermes__config_without_trailing_newline",
    "openclaw__fresh",
    "openclaw__rerun",
    "openclaw__empty_key",
    "openclaw__existing_settings_preserved",
    "openclaw__model_policy_widened",
    "openclaw__empty_model_policy_left_alone",
    "openclaw__model_policy_wildcard_already_allows",
    "openclaw__model_entry_string_normalised"
  ],
  "comparator": "agent-config-files",
  "comparator_notes": {
    "agent-config-files": "Each case runs `cli::integrations::configure(agent, api_url, model, api_key)` against a throwaway home directory. `input.seed_files` are written into that home (parent directories created) BEFORE the call; `input.prior_runs`, when present, lists earlier `configure` invocations made in the same home, in order, before the recorded one — that is how rerun/idempotence cases are expressed. `expected.files` is the COMPLETE set of files present under the fake home afterwards: every path is relative to that home, uses forward slashes, is sorted, and its value is the file's exact text — compare byte-exact, trailing newline included. A path absent from `expected.files` must not exist after the port runs. `expected.ok` is false when `configure` returned Err, and `expected.error` then carries the message verbatim.",
    "cline": "Cline writes no config file at all — it runs `cline auth …` as a subprocess. Those cases put a recording stub named `cline` on PATH, so `expected.spawn` is the exact argv the writer passed, `expected.files` is empty, and the failure/missing cases show the error text. A port must reproduce the argv, not a file.",
    "paths": "Relative to the fake home ($HOME on unix, %USERPROFILE% on Windows — `agent_home_dir()`). The emitter also pins $DSH_HOME to `<home>/.dsh` (its default) so dsh never probes a login shell, and clears $OPENCLAW_CONFIG_PATH, $ATOMIC_AGENT_STATE_DIR and $HERMES_HOME so every writer uses its default location.",
    "placeholders": {
      "<home>": "the fake home directory the case ran against; substituted anywhere its absolute path appears in a file's contents",
      "<os-error>": "the platform-specific std::io::Error text after `Failed to spawn 'cline': `",
      "<timestamp>": "a wall-clock RFC 3339 instant written by the agent writer (only `createdAt` in OpenClaude's profile file)"
    },
    "shell": "`input.shell` is $SHELL during the run. The four env-var agents (copilot, goose, openhands, muse, poolside) pick their rc file from it: a value ending in `/bash` selects ~/.bash_profile on macOS and ~/.bashrc on Linux, anything else selects ~/.zshenv. These fixtures were emitted on a host where cfg!(target_os) = macos, so the bash cases show ~/.bash_profile; on Linux the same writer targets ~/.bashrc."
  },
  "source": {
    "commit": "ccf542733",
    "file": "src-tauri/src/core/cli/fixture_dump.rs"
  }
}

# Critical flows — test evidence

An evidence map, not a coverage dashboard. Grades (same rules as the app):

- **Strong** — a test enters through a production entry point, asserts an observable outcome, covers a
  failure path, and crosses a real layer boundary (process, file system, socket).
- **Partial** — two or three of the above.
- **Smoke** — the code runs and does not throw.
- **Missing** — no test.

Line coverage alone never raises a grade. A PR may not lower a grade.

| Flow | Grade | Evidence | Phase target |
| --- | --- | --- | --- |
| CLI: version, usage, dispatch, exit codes | Strong | `src/cli/main.test.ts` (dispatch to every phase-1 command, a full daemon → serve → shutdown cycle in one process), `src/cli/commands.test.ts`, `test/e2e/{binary,owner}.test.ts` on the compiled binary | maintained |
| Rust contract parity: argv, error cascade, runtime-device, `--list-devices` | Strong (contract) | `test/contract/{args,errors,runtime-device,devices}.test.ts` replay every fixture in `test/fixtures/app/` with the comparator the emitter names; checksum shared with the app | maintained; the three shim sets replay since stage 4a (`test/contract/shims.test.ts`), `proxy-http` and `state-file` since stage 4b (`test/contract/{proxy-http,state-file}.test.ts`, also under Bun) |
| Process spawn, readiness markers, timeout, exit-code capture, kill | Partial | `src/runtime/process.test.ts` (real child processes: ready line, health poll, `MODEL_LOAD_TIMED_OUT`, missing exe, SIGTERM→SIGKILL), `test/runtime-compat/spawn.test.ts` | Strong in phase 1 (fake-llama-server) |
| Load a llama.cpp model (23-step plan → argv → readiness) | Strong | `load-plan.test.ts` (pure plan), `runtime.test.ts` (real processes: readiness, device logs, journal failure rollback, auto-unload, chained projector→MTP fallback, startup cancellation, log relay), `test/live/llamacpp.test.ts` against a real `llama-server` + GGUF | maintained; MLX and Foundation Models in phase 5 |
| Stage 5 runtimes: TurboQuant (`llamacpp`) | Strong (core), Partial (desktop) | `test/contract/args.test.ts` over `args-llamacpp` (70 Rust fixtures), `src/backend/turboquant.test.ts` (fork matrix, ROCm probe, categories, fork-tag sort, assets, cudart repair copy/download), `src/backend/{service,runtime-backend}.test.ts` (install source, selection, repair hook), `core.test.ts` (fork build selected and served); live `test/live/turboquant.test.ts` on a real `b10269-1.6.0` fork: process started with `--cache-type-k/v turbo3`, answers via `/v1`, context grown; extension `llamacpp-extension/src/test/coreOwned.test.ts` (settings handover, load/unload/sessions/chat/ctx/devices/backend install through the core) | desktop acceptance with `runtime=all` |
| Stage 5 runtimes: MLX | Strong (core, fake server), Missing (live) | `test/contract/mlx.test.ts` (40 Rust fixtures: argv and stderr cascade), `src/runtime/mlx/*.test.ts` (fake `mlx-server`: argv, drafter, auto-unload, ladder, errors, shard repair, died), `core.test.ts` (public API routes MLX and grows its context), `src/cli/commands.test.ts` (`serve --provider mlx`); `test/e2e/stage5-sidecars.test.ts` checks overlapping load/unload, child exit and claim release on the compiled macOS app core; extension `mlx-extension/src/coreOwned.test.ts`. `test/live/mlx.test.ts` exists but was not run: no real `mlx-server` or MLX model on the development machine | live run on Apple Silicon with a small MLX model |
| Stage 5 runtimes: Foundation Models | Partial | `test/contract/foundation-models.test.ts` (11 fixtures, one deliberate correction), `src/runtime/foundation-models/*.test.ts` (fake server: key, fail-fast reason line, timeout, died, `--check` cache), `test/e2e/stage5-sidecars.test.ts` checks overlapping load/unload, child exit and claim release on the compiled macOS app core; app live `the_core_finds_the_sidecar_servers_the_app_bundles`; live `test/live/foundation-models.test.ts` on a real server built from the app sources: with Apple Intelligence disabled the load is refused as `FOUNDATION_MODELS_UNAVAILABLE` in under a second. The answering path was not run (Apple Intelligence off) | live run on a Mac with Apple Intelligence enabled |
| Runtime ownership `atomic_core.runtime = all` and rollback | Partial | app `models.rs` (flag value → four providers), `commands.rs` (`changing_providers`: off→all, upstream→all, all→upstream; a load lease blocks handover), `external.rs` (registration withdrawn when the core owns everything), `web-app` `model-factory-session-resolver.test.ts` (Foundation Models session from the resolver first), `download-extension` (core cancel under `all`), `tests/extension-surface.test.mjs` (every migrated method reads ownership). A real flag flip in a running Tauri app, including a load in progress and models on each side, is not covered end to end | desktop acceptance |
| Unload and session died | Strong | `runtime.test.ts` (SIGTERM→SIGKILL, journal cleared, `session:died` after an external kill), `core.test.ts` (shutdown stops every session), `test/e2e/owner.test.ts` | auto-increase-ctx in phase 3b |
| Cancel a model load (stage 7a) | Strong (core), Missing (desktop) | `src/runtime/shared/load-cancel.test.ts` (the five semantics of the app's `load_cancel.rs` plus the shared entry), `process.test.ts` (an already-cancelled load never spawns; a SIGTERM-ignoring child is dead before the error is raised; owner shutdown keeps `CORE_NOT_RUNNING`), `sidecar.test.ts` and `llamacpp/runtime.test.ts` (a queued cancel ends at once and the load behind it still waits for the one ahead; a cancel that raced readiness publishes nothing; no projector/MTP retry; a joiner is rejected while the load continues; planning blocked on an install does not block the cancel), `mlx` and `foundation-models` runtime tests on the fake sidecar, `src/core/sessions-cancel.test.ts` (every ordering of cancel against the acquire: both pending acquires rejected, cancel wins until the answer is decided then answers `false`, claim kept over a process that would not stop, a queued unload runs next, a body `signal` key ignored; a real core with a backend that never becomes ready), `routes/models.test.ts` and `control-client.test.ts` (route, 409, client), `test/e2e/load-cancel.test.ts` on the compiled binary (409 with the app code, child gone first, empty sessions, journal and claims; queued cancel leaves the load ahead alone) | desktop acceptance: cancel from the app's load snackbar on a real multi-gigabyte model |
| Backend manifest, selection, install, update, optimal cache | Partial | `src/backend/*.test.ts` (mirror/fallback, install staging, revisioned file CAS/409 and old-cache migration), `test/e2e/owner.test.ts` (compiled owner + actual local TLS proxy/manifest/mirror, checksum failure, ggml-org fallback, cancel, SSE progress, Windows CUDA companion), app `live_tests.rs` (optimal snapshot then SSE and new generation); no UI install/update/cancel acceptance | Strong after real desktop UI acceptance |
| Core-owned batched embeddings | Partial | `src/models/embed.test.ts` (existing/absent session, batching and global indices, one 501 reload, errors), `test/e2e/owner.test.ts` (compiled owner, fake backend returns 501 until reload in embedding mode, two batched inputs and one replacement process); no live RAG request through a running app | Strong after live RAG acceptance |
| Resumable download + verification + disk tags | Strong | `src/downloads/downloader.test.ts` against `test/helpers/fixture-http-server.ts` (Range/206, 200/416 restart, drops, retries, cancel keeps partials, sha256 mismatch, cleanup of file and empty dir, `[disk_*]` preflight) | maintained |
| Download stages: what a download is waiting on (stage 7b) | Strong (core), Missing (desktop) | `src/downloads/downloader.test.ts` against the fixture server (`connecting` only when a HEAD is made, `retrying` 1…5 in the preflight and first-request ladders, the ladder's limit, silence during a mid-transfer reconnect, a stage never travels as a progress frame), `protocol.test.ts` (shape), `test/e2e/owner.test.ts` on the compiled binary (a mirror answering 503 twice: two `download:stage` SSE frames on the caller's task id, then the install completes) | the app's relay mapping onto `download-<taskId>` and the "Retrying n/5" row in a running app |
| Free disk space inside the data folder (stage 7b) | Strong | `src/downloads/disk-space.test.ts` (real probe, a target that does not exist yet, `null` when unknown, relative/empty/NUL/non-string refused, outside and `..` refused, a symlink that leaves the folder), `routes/disk.test.ts` (path only from the body, 400, 405 for GET), `control-client.test.ts`, `test/e2e/owner.test.ts` on the compiled binary | maintained |
| Proxied downloads: HTTP forward, CONNECT, SOCKS5, proxy auth, `no_proxy`, `ignore_ssl`, Range through the tunnel, abort, redirects | Strong | `src/downloads/proxy-fetch.test.ts` and `test/runtime-compat/proxy-fetch.test.ts` against real loopback proxies; abort is covered during headers, CONNECT, SOCKS and TLS, and truncated Content-Length/chunked bodies reject | compiled-binary run in phase 1 |
| Hugging Face GGUF discovery and download | Strong | `src/models/hf.test.ts` covers repo validation, token precedence/auth, metadata normalization, Q4/largest selection, unsafe filenames, gated/empty errors, sha256 validation and publishing `model.yml` only after success | maintained |
| Settings store: schema, canonical keys, revisions, change events | Partial | `src/settings/store-import.test.ts` (three-way legacy import, conflict, idempotent and stale acknowledgement); `test/e2e/owner.test.ts` imports, edits through control, resolves a 409, acknowledges, restarts a compiled owner and checks `in_sync` before/after another edit. The post-write revision defect is fixed; desktop downgrade flow is not covered. | Run real desktop rollback acceptance |
| Speculative registries (DFlash / MTP / EAGLE-3 / transcription / template overrides) | Partial | `src/speculative/*.test.ts` (table-driven resolution, verbatim port) | Strong in phase 5 |
| Forwarding `/v1` to a session (models, completions, SSE, cancel, ctx retry, `/messages` fallback, `/responses`) | Strong | `test/contract/proxy-http.test.ts` (78 recorded Rust exchanges over raw sockets: local/remote routing, alias, 404/503, unreachable 503/502, structured errors, ctx-overflow and finish-length retries, compute-error 400, Anthropic fallback whole and streamed, Responses translate/passthrough, metrics); `src/server/public/*.test.ts` (client hang-up tears the upstream down, idle timeout, one shared reload for concurrent overflows, embeddings never grown, remote failures); `core.test.ts` (fake llama-server overflows until the core grows the context and replays; poisoned engine restarted at the same context); `test/e2e/owner.test.ts` (stream and cancel through the compiled binary), `test/e2e/public-server.test.ts` (compiled binary + fake llama-server processes: cold 503 vs unknown 404 vs malformed 400, context grown and request replayed, fit declines with the engine's error, poisoned engine restarted with 400 `insufficient_memory`, `/messages` fallback whole and streamed, `/responses` translated whole and streamed, embeddings, metrics, model and Muse listings, killed backend → 503 `backend_unavailable` + `Retry-After`) | maintained |
| Server gates: host, api key, CORS, preflight | Strong (contract) | `test/contract/proxy-http.test.ts` (default and trusted hosts, `*`, missing Host, IPv6 loopback refused, Bearer/`X-Api-Key`, whitelisted docs, `/configs` hidden, origin reflection, preflight method/header/host rules), `src/server/public/gates.test.ts`, `http.test.ts` (control listener guards), `test/e2e/public-server.test.ts` (key, trusted hosts, missing Host, hidden `/configs`, 405, preflight and origin reflection configured through control on the compiled binary; Swagger assets served byte-identical from the binary) | maintained |
| Dynamic trusted hosts: the tunnel name and the accepted socket's address (stage 7c) | Strong (core) | `src/server/public/dynamic-hosts.test.ts` (the five `dynamic_hosts.rs` tests against the core's `isValidHost`; `*`, URLs, ports and paths can never become the tunnel name; every socket-address spelling: mapped, hex-mapped, zone id, bracketed IPv6, loopback and unspecified in both families), `test/contract/dynamic-hosts.test.ts` on a real listener under Node and Bun (403 → 200 with the origin reflected → 403; another `*.trycloudflare.com` stays a stranger; the same keep-alive connection is refused before and accepted after; preflight; a listener on `0.0.0.0` trusts the dialled LAN address, refuses a rebinding name on that socket and the LAN literal over loopback; a loopback bind gains nothing), `src/core/public-server.test.ts` (a real core, and a repeated start stays idempotent), the `proxy-http` set unchanged | a phone on the same Wi-Fi reaching a running app |
| Remote access: the Cloudflare quick tunnel (stage 7d) | Strong (core, fake cloudflared and a stand-in edge), Missing (live) | `src/remote-access/manager.test.ts` (the app's 19 state-machine tests plus: a stuck supervisor loses the state, a second stop joins the first, a process that came up after shutdown is not kept, the process-exit guard), `process.test.ts` on a real child (URL + registration, URL only, silence, early exit, exit after ready, SIGTERM ignored then killed, an exit that cannot be confirmed, Windows goes straight to the kill, a program that cannot start, real argv and no `TUNNEL_*`), `cloudflared-{args,output}.test.ts` (tables), `probe.test.ts` (ours / foreign / unreachable, the marker against the real public server, edge by SNI with certificate verification on the tunnel name, 1033 until the tunnel is there, two silent rounds give up the edge, hostname fallback, abort), `journal.test.ts` (write-then-enrich, never resurrected after a clear, identity *and* name before a kill, start-time fallback, a real child ended and a real non-tunnel spared), `spawner.test.ts`, `wiring.test.ts` (test hooks), `src/core/remote-access.test.ts` on a real core (Host gate 403 → 401 with the key → 403 as the tunnel comes and goes, tunnel ended before `server:stopped`, an unchanged start leaves it alone, HTTP/2 retry with real processes, shutdown kills it, no binary), `src/core/public-server.test.ts`, `routes/remote-access.test.ts`, `control-client.test.ts`; `test/runtime-compat/address-pin.test.ts` under Node and Bun (a Node edge process reports the SNI the client really sent); `test/e2e/remote-access.test.ts` on the compiled app-core binary (refusal, online through the stand-in edge, journal, stop, ordering over SSE, HTTP/2 retry with the real 15 s, missing binary, a crash's journal consumed by the next owner, shutdown) | a real `cloudflared` against Cloudflare from a running app; a phone opening the URL |
| Image generation on stable-diffusion.cpp (stages 7e–7g) | Strong (core, fake engine), Missing (routes, live engine, desktop) | pure ports with the plugin's `#[test]` tables: `src/diffusion/{args,progress,tracker,workflow,parse,validate,errors,recipe}.test.ts` (`args.test.ts` holds the argv to `test/fixtures/sdcpp/required-flags.txt`); disk: `png.test.ts` (fixtures from an independent encoder, Pillow-checked; damaged and unsupported files refused), `gallery.test.ts` (foreign PNGs never listed or deleted, flags file, atomic writes, export), `install.test.ts` (finalize probes a real script, owned trees only, model files inside the root), `containment.test.ts` (symlinks out of and into the root); process and jobs: `server-process.test.ts` on the fake engine (journalled before ready, capabilities, early exit with marked lines first, OOM, timeout kills, foreign listener stopped, abandoned load stopped), `jobs.test.ts` against an HTTP stub (the `jobs.rs` runner tests: completion with outputs and every transition, failures explained by the output, queue full, forgotten job, cancel ladder incl. the second grace, cancel wins over a waiting respawn, crash mid-job with the spec kept, ggml-abort CPU retry, transport errors, ceiling), `session.test.ts`, `idle.test.ts`, `service.test.ts` (the real fake engine: finalize → load → generate with progress → gallery → cancel → respawn → idle unload → shutdown, child dead each time) | routes and facade (7h), `test/e2e/diffusion.test.ts` on the compiled binary and `test/live/diffusion.test.ts` on a real engine (7j), desktop acceptance from the Images page |
| LAN addresses for display (stage 7c) | Strong | `src/remote-access/lan.test.ts` (the five `lan.rs` tables: default route first, undialable dropped, virtual adapters per OS, CGNAT kept on `utun`), `lan-probe.test.ts` (interfaces in a stable order, every failure of the default-route lookup closes the socket and answers nothing, this machine yields only dialable literals), `routes/remote-access.test.ts`, `src/core/create.test.ts` | maintained |
| Local API Server handover app ↔ core (`atomic_core.server=core|legacy`) | Strong (app side), Partial (UI) | app `server/ownership.rs` unit (nothing serving → flag only; stop before start; failed start restores the previous owner; double failure reports stopped; unknown config or unstoppable server left untouched; lost `/server/stop` response is rechecked; providers handed over before `/server/start` with `state_file` and `fallback_port`); app live `live_tests.rs` against the compiled core (the core binds the very port the app released, serves the handed-over cloud provider, writes `local-api-server.json`, and hands back marking it stopped; an unbindable address restores the app server or reports stopped); core `core.test.ts` (state file only when handed the server, free-port fallback idempotent); extension `autoIncreaseCtx.test.ts` (compute recovery for a core-owned model goes to `/recreate`). Recovery uses one attempt per generation and emits confirmed state, but the real proxy (`LegacyOwner`) inside a running Tauri app and the webview Launch port during a flag change are not covered | desktop acceptance |
| ChatGPT OAuth + token refresh + subscription route | Strong | `test/contract/chatgpt.test.ts` (Rust fixtures: PKCE, authorize URL, callback parsing, JWT claims, token response → session, token file bytes and mode, upstream request, model normalisation); `src/credentials/chatgpt-auth.test.ts` (real loopback callback listener: code exchange with verifier, state mismatch, provider error, cancel, busy port, timeout; single-flight refresh; tokens rotated by another process used instead of a second refresh; terminal refusal signs out only if the file still holds the refused token); `src/cloud/chatgpt.test.ts` (aggregate and stream, one forced refresh on 401, verbatim quota errors); `test/e2e/cloud.test.ts` (compiled daemon: sign-in through control against a stub issuer, subscription streamed after a 401 refresh, token file rewritten 0600, CLI status/logout, 401 once signed out). Not covered: the real `auth.openai.com` and `chatgpt.com` (live-cloud suite), the app's webview login flow through the core | live-cloud cassettes |
| Instance lock, concurrent launch, CLI attach, PID reuse | Strong | `src/lock/*.test.ts` (four racing starters yield one owner; dead pid, reused pid and unprovable identity each handled; stale takeover mutex), `src/cli/owner.test.ts`, `test/e2e/owner.test.ts` (two simultaneous `serve` launchers converge on one daemon; second explicit owner refused) | maintained |
| Independent control/public listeners; client detach and explicit shutdown | Strong | `src/server/control.test.ts`, `core.test.ts` (idempotent compatible starts, incompatible conflict preserves traffic, bind failure, lifecycle rejects new work), `runtime.test.ts` (in-flight startup cancelled and child killed), `test/e2e/owner.test.ts` (two clients, explicit shutdown, no orphan) | maintained |
| Legacy resource guard; reaper preserves live owner and backend | Missing | — | Strong before distribution in phase 2 |
| Crash cleanup; snapshot/SSE resync | Partial | `lock/process-journal.test.ts` + `core.test.ts` (orphan policy), `test/e2e/owner.test.ts` (killed owner and backend reap), `control.test.ts` and `client/control-client.test.ts` (cursor replay/resync); app `make test-core-live` (production relay starts owner, recovers after kill, snapshot precedes deltas, replacement generation and fourth restart refused). Webview state after resync is not tested. | Strong after app-UI snapshot/resync acceptance |
| All session consumers and registered legacy TurboQuant/MLX/FM capabilities | Partial | `src/runtime/external-sessions.test.ts` (generation refusal, heartbeat expiry, validation, ctx request answered/timed out/owner gone); `core.test.ts` (registered MLX session routed by alias, context grown through the owner, unregister → 503); `test/e2e/api-events.test.ts` (compiled daemon: stale generation 409, TurboQuant session routed with its key, ctx request answered over control); app `external.rs` unit tests. Not covered: the app's publisher loop against a live core, Foundation Models (never routed by the proxy) | Strong after the app live suite drives the publisher |
| API request analytics and inspector with the core serving | Strong | `test/contract/inspector-telemetry.test.ts` (Rust fixtures), `src/server/public/trace.test.ts` (observation for product traffic only, no previews unless watching, usage trailer injected and stripped, progress, abort, whole-body and SSE-tail telemetry, /responses), app `request_inspector.rs` `core_ingest_tests` and `api_request_analytics.rs` (closed label sets), `test/e2e/api-events.test.ts` (compiled daemon: observation without previews, previews once the inspector is on) | maintained |
| Final signed universal artifact executes on macOS arm64 and x64 | Missing | — | Strong in phase 2 |
| Control API over the wire (auth, host gate, snapshot, clients, SSE) | Strong | `src/server/control.test.ts` and `src/client/control-client.test.ts` drive a real server over a socket; `test/e2e/owner.test.ts` does the same against the compiled binary | maintained |
| Mobile paths survive desktop legacy removal | Missing | — | Existing mobile build/contract checks in phase 6 |
